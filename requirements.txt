# NeuroLearn — Project Requirements

## System Requirements
- Node.js (v18+)
- Python (v3.9+)
- Firebase CLI (`npm install -g firebase-tools`)
- Vercel CLI (`npm install -g vercel`)

## Python Dependencies (for neurolearn-ml)
# Install via: pip install -r requirements.txt
fastapi==0.111.0
uvicorn[standard]==0.29.0
opencv-python-headless==4.8.1.78
scikit-learn==1.3.2
numpy==1.24.4
Pillow==10.1.0
joblib==1.3.2
httpx==0.27.0
python-multipart==0.0.9
pydantic==2.7.0
google-cloud-storage

## Node.js Dependencies (Primary)
# Note: These are installed via 'npm install' in respective folders.
# Frontend: react, vite, firebase, @tensorflow/tfjs, @google/generative-ai, recharts, zustand
# API: firebase-admin, @google/generative-ai
